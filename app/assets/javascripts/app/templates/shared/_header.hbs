{{!-- TODO: This should be its own view so we can bind events in one place--}}
<header class="page-head board-head">
  <nav>
    <div id="logo">
      <h1>Mrello</h1>
    </div>
    {{#if session }}
      <div id="search-bar">
        <label for="search-bar">
          <input class="button-outline" type="text" name="search-bar">
          <i class="glyphicon glyphicon-search"></i>
        </label>
      </div>
    {{/if}}
  </nav>

  {{#if session}}
  <nav id="nav-aside">
      {{!-- TODO: Should only be available if Admin --}}
      <div id="create-org" class="btn">
        <a href="#">Create Organization</a>
      </div>

      {{!-- TODO: Should only be availiabe if not Admin --}}
      <div id="join-org" class="btn">
        <a href="#">Join Organization</a>
      </div>

      <div id="logout" class="btn">
        <a href="#">Logout</a>
      </div>
  </nav>
  {{/if}}
</header>